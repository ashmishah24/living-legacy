{
  "name": "living-legacy",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "dev:web": "pnpm --filter @living-legacy/web dev",
    "dev:mobile": "pnpm --filter @living-legacy/mobile start",
    "build:web": "pnpm --filter @living-legacy/web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.8.3"
  }
}

